<style>
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  
  .navigation {
    backdrop-filter: blur(10px);
    padding: 10px;
    font-weight: bold;
    text-align: center;
    justify-content: space-between;
    display: flex;
    border-radius: 10px;
    margin-top: 1%;
    margin-bottom: 1%;
    z-index: 1000;
  }
  
  .left-nav,
  .right-nav {
    
    display: flex;
  }
  
  .right-nav {
    margin-left: auto;
  }
  
  .navigation a {
    color: #ffffff;
    width: 120px;
    margin-left: 15px;
    font-family: "Lexend Deca", sans-serif;
    font-weight: 350;
    text-decoration: none;
    padding: 5px 10px;
    border-style: dotted;
    border-color: #00000000;
    border-radius: 90px 20px 60px 90px / 50px 20px 38px 20px;
    display: inline-block;
    transition: transform 0.1s ease, text-shadow 0.3s ease, background-color 0.1s ease, color 0.1s ease, box-shadow 0.3s ease;
  }
  
  .navigation a:hover {
    transform: scale(1.2);
    color: white;
    box-shadow: inset 40px -30px 0px rgba(255, 0, 119, 0.993);
    text-shadow: 0 0 8px rgba(255, 255, 255, 0.8), 0 0 16px rgba(0, 0, 0, 0.6);
  }
  
  .navigation a:active {
    box-shadow: inset 40px -90px 0px rgba(255, 174, 0, 0.993);
    transform: scale(1.2);
  }
  
  .favicon {
    filter: invert();
    width: 40px;
    height: 40px;
    margin: 0 1%;
    transition: transform 0.5s ease, opacity 0.3s ease;
  }
  
  .favicon:hover {
    transform: scale(1.1);
    opacity: 0.8;
  }
  
  .dividerN {
    border: none;
    border-top: 2px solid #ffffff;
    margin: 20px 20px;
    width: 100%;
  }
  
  /* Responsive Design */
  @media (max-width: 768px) {
    .navigation {
      display: none;
    }
  
    .hamburger-menu {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px;
      
      border-radius: 10px;
    }
  
    .hamburger-menu .menu-icon {
      
      border-radius: 50px;
      cursor: pointer;
      font-size: 1.5rem;
      color: #ffffff;
      z-index: 1001;
      transition: all 0.3s ease;
    }
    .hamburger-menu .menu-icon:hover {
      transform: rotate(90deg);
      
    }
    .hamburger-menu .menu-icon:active {
      
      color: rgb(38, 0, 255);
     
    }
    
    .mobile-menu {
  display: none; /* Hide by default */
  flex-direction: column;
  align-items: left;
  
  background-color: rgba(255, 0, 119, 0.993);
    color: white;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  padding-left: 15%;
  padding-top: 0%;
  z-index: 1000;
  justify-content: center;
  transition: all 0.3s ease;
}

.mobile-menu a {
  margin: 10px 0;
 
  font-size: 2.5rem;
  transition: all 0.3s ease;
  color: white;
  text-decoration: none;
  font-weight: bold;
}
    .mobile-menu a:hover {
      
      transform: translateX(1%);
    }
    
  }
  .mobile-menu.slide-in {
   animation: slide 0.3s ease;
 
  }
  @keyframes slide {
  0% {
    
    transform: translateX(10%);
  
  } 
  50% {
    transform: translateX(0%);
  
  } 
  100%{
    font-size: 2.5rem;
  }
}
@keyframes slideout {
  0% {
    transform: translateX(0%);
  
  } 
  50% {
    background-color: rgba(255, 217, 0, 0.993);
    transform: translateX(-100%);
    
  } 
  100%{
    
    transform: translateX(-100%);
  }
}

  .mobile-menu.slide-out {
    animation: slideout .5s ease;
  }
  
  /* Force-hide mobile menu on larger screens */
  @media (min-width: 769px) {
    .mobile-menu {
      display: none !important;
    }
    .hamburger-menu {
      display: none !important;
    }
    .favicon{
      transform: translateX(-50%);
    }
  }
  .title {
    font-family: "Lexend Deca", sans-serif;

    margin: -2rem 0;
    font-size: 6.5rem;
    font-weight: 700;
  }
  </style>
  
  <div class="hamburger-menu">
    <img src="/public/initials.png" alt="Logo" class="favicon">
    <span class="menu-icon">▶</span>
  </div>
  
  <nav class="navigation">
    <div class="left-nav">
      <img src="public/initials.png" alt="Logo" class="favicon">
      <a href="/">| HOME</a>
      <a href="/about/">| ABOUT</a>
    </div>
    <div class="right-nav">
      <a href="/gallery/">| GALLERY</a>
      <a href="/projects/">| PROJECTS</a>
    </div>
    <hr class="dividerN">
  </nav>
  
  <div class="mobile-menu">
    <p class="title">NAV</p>
    <a href="/">| HOME</a>
    <a href="/about/">| ABOUT</a>
    <a href="/gallery/">| GALLERY</a>
    <a href="/projects/">| PROJECTS</a>

  </div>
  
  <script>
 const menuIcon = document.querySelector('.menu-icon');
const mobileMenu = document.querySelector('.mobile-menu');
let menuVisible = false;

// Toggle the visibility of the mobile menu
menuIcon.addEventListener('click', () => {
  if (!menuVisible) {
    mobileMenu.classList.add('slide-in');
    mobileMenu.classList.remove('slide-out');
    mobileMenu.style.display = 'flex';
  } else {
    mobileMenu.classList.add('slide-out');
    mobileMenu.classList.remove('slide-in');
    setTimeout(() => {
      mobileMenu.style.display = 'none'; // Ensure it's hidden after animation
    }, 300); // Matches the transition duration
  }
  menuVisible = !menuVisible;
});

  </script>
  